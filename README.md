# AWS_flask_ML_project
its is ML Project, using jupyter notebook, flask and AWS tool for predicting whether the customere is eligible for loan, flask is used to feed the details such as age, ehich lean he is applying for, duration of the loan, does the applicant have paying any rent now(own any house).
